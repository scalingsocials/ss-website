---
/**
 * /lp/performance-marketing/ — ad landing page (noindex, paid traffic only).
 * Excluded from the sitemap and orphan check; not linked from the site.
 */
import LandingLayout from '@/layouts/LandingLayout.astro';
import { LANDING_BY_SLUG } from '@/lib/landings';

const content = LANDING_BY_SLUG['performance-marketing'];
if (!content) throw new Error('Missing landing content: performance-marketing');
---
<LandingLayout content={content} />
